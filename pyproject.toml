[project]
name = "personal-investment-analytics"
version = "0.1.0"
description = "A local personal investment analytics and strategy platform."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
